<% content_for :title, "Connections — My Social Practice" %>

<div class="mb-6">
  <h1 class="text-2xl font-semibold tracking-tight text-slate-900">Connections</h1>
  <p class="mt-1 text-sm text-slate-500">Agency-wide API credentials used to pull data for every client's report.</p>
</div>

<div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
  <%= render partial: "connections/connection_card", collection: @connections, as: :connection %>
</div>
